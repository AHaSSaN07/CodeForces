#include <bits/stdc++.h>
using namespace std;
int main()
{
	string input;
	int itr;
	cin >> input;
	for (int i = 0; i < input.size(); i++)
	{
		cout << input[i];
		itr = i;
	}
	for (int i = itr; i >= 0; i--)
	{
		cout << input[i];
	}

}
