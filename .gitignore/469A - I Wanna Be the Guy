#include <bits/stdc++.h>
using namespace std;

int main()
{
	set<int> levels;
	int n,p,x;
	cin >> n;
	cin >> p;
	int a;
	for (int i = 0; i < p; i++)cin >> a, levels.insert(a);
	cin >> x;
	for (int i = 0; i < x; i++)cin >> a, levels.insert(a);
	if (levels.size() == n)cout << "I become the guy.\n";
	else cout << "Oh, my keyboard!\n";

}
